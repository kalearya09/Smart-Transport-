name: smart_route_app
description: A sample command-line application with basic argument parsing.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.8

# Add regular dependencies here.
dependencies:
  flutter_map: ^6.0.0
  latlong2: ^0.9.0
  http: ^1.2.0
  args: ^2.7.0
  flutter_typeahead: ^5.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

